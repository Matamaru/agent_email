# AGENTS.md

Repo focus: agent_email.

Guidelines:
- Keep contracts and public APIs stable; update docs when behavior changes.
- Prefer deterministic, testable behavior over dynamic side effects.
- Run tests and lint before releases (use Makefile if present).
